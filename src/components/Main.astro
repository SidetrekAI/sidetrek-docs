---
import PageBreadCrumb from './PageBreadCrumb.astro'
import { HEADER_HEIGHT } from '@/lib/constants'
---

<main class="px-6 py-6 md:px-10 w-full mx-auto text-slate-800" style={{ height: `calc(100vh - ${HEADER_HEIGHT}px)` }}>
  <div class="container mx-auto">
    <PageBreadCrumb />

    <slot />
  </div>
</main>
